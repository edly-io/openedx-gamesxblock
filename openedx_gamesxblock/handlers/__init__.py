"""
Handler classes for the Games XBlock, grouped by game type.

Each handler class exposes ``student_view`` (renders the block for learners)
plus whatever submit/complete handlers that game needs. Common authoring
handlers (studio_view, save_settings, upload_image) live in ``common.py``.
"""
